@model IEnumerable<AgenteMarketingTeste.Models.VendaDetalheContato>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProdutoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProdutoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VendaDetalheContatoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VendaDetalheContatoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VendaDetalheContatoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
